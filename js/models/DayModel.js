var DayModel = function(date, dts) {
    this.date = date;
    this.dts = dts || [];
    this.user = user;

    model.getDate = function() {
        return this.date;
    }

    model.setDate = function(date) {
        this.date = date;
    }

    model.getDays = function() {
        return this.days;
    }

    model.setDays = function(days) {
        this.days = days;
    }

    model.

    model.save = function() {

    }

    return model;
}
